{% extends 'template.html.twig' %}

{% block page %}
    <section class="head light">
        <div class="BGcolor">
            <h2 class="head-title">Éclairages</h2>
        </div>
    </section>
    <section class="main-content liste-materiel">
        <div class="liste-elm">
            {% for eclairage in eclairages %}
                <div class="elm">
                    <div class="img-container">
                        <img src="{{ eclairage.IMAGE }}" alt="" class="elm-pic">
                    </div>
                    <span class="elm-name">{{ eclairage.NOM }}</span>
                    <a href="index.php?page=materiel&id={{ eclairage.ID }}" class="btn btn-materiel" data-content="Réserver">Réserver</a>
                </div>
            {% endfor %}
        </div>
    </section>
{% endblock %}