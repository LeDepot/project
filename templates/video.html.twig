{% extends 'template.html.twig' %}

{% block page %}
    <section class="head video">
        <div class="BGcolor">
            <h2 class="head-title">Vidéos</h2>
        </div>
    </section>
    <section class="main-content liste-materiel">
        <div class="liste-elm">
            {% for video in videos %}
                <div class="elm">
                    <div class="img-container">
                        <img src="{{ video.IMAGE }}" alt="" class="elm-pic">
                    </div>
                    <span class="elm-name">{{ video.NOM }}</span>
                    <a href="index.php?page=materiel&id={{ video.ID }}" class="btn btn-materiel" data-content="Réserver">Réserver</a>
                </div>
            {% endfor %}
        </div>
    </section>
{% endblock %}